# Руководство для контрибьюторов

Спасибо за интерес к проекту "Искусанный Интеллектом Маркетолух"! Мы рады любому вкладу в развитие проекта.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Настройка окружения разработки](#настройка-окружения-разработки)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кодирования](#стандарты-кодирования)
- [Тестирование](#тестирование)
- [Документация](#документация)
- [Процесс ревью кода](#процесс-ревью-кода)

## 🤝 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать наш Кодекс поведения:

- Будьте доброжелательны и уважительны к другим участникам
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам сообщества

## 🎯 Как внести вклад

### Типы вкладов

Мы приветствуем различные типы вкладов:

1. **Исправление багов** - найдите баг в Issues или создайте новый issue
2. **Новые функции** - предложите новую функцию через Discussion
3. **Улучшение документации** - исправьте опечатки, добавьте примеры
4. **Рефакторинг кода** - улучшите качество существующего кода
5. **Тесты** - добавьте новые тесты или улучшите существующие

### Процесс внесения изменений

1. **Форкните репозиторий**
   ```bash
   # Форкните через GitHub UI
   # Затем клонируйте ваш форк
   git clone https://github.com/ваш-username/Trial_RDV.git
   cd Trial_RDV
   ```

2. **Добавьте upstream remote**
   ```bash
   git remote add upstream https://github.com/RDmitryV/Trial_RDV.git
   ```

3. **Создайте новую ветку**
   ```bash
   git checkout -b feature/название-фичи
   # или
   git checkout -b fix/описание-бага
   ```

4. **Внесите изменения и закоммитьте**
   ```bash
   git add .
   git commit -m "feat: краткое описание изменений"
   ```

5. **Синхронизируйте с upstream**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

6. **Запушьте изменения**
   ```bash
   git push origin feature/название-фичи
   ```

7. **Создайте Pull Request**
   - Перейдите на GitHub
   - Нажмите "New Pull Request"
   - Заполните шаблон PR
   - Дождитесь ревью

## 🛠️ Настройка окружения разработки

### Требования

- Python 3.11+
- Node.js 18+ и npm
- Docker и Docker Compose
- Git
- PostgreSQL 14+ (опционально для локальной разработки)
- Redis (опционально для локальной разработки)

### Быстрый старт

#### Вариант 1: Использование Docker (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/RDmitryV/Trial_RDV.git
cd Trial_RDV

# Скопируйте и настройте переменные окружения
cp .env.example .env
# Отредактируйте .env файл с вашими ключами API

# Запустите все сервисы
docker-compose up -d

# Примените миграции базы данных
docker-compose exec backend alembic upgrade head

# Откройте браузер
# Backend API: http://localhost:8000/docs
# Frontend: http://localhost:3000
```

#### Вариант 2: Локальная установка

**Backend:**

```bash
cd backend

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
pip install -r requirements-dev.txt

# Настройте переменные окружения
cp ../.env.example ../.env
# Отредактируйте .env

# Примените миграции
alembic upgrade head

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**

```bash
cd frontend

# Установите зависимости
npm install

# Скопируйте переменные окружения
cp .env.example .env

# Запустите dev сервер
npm run dev
```

## 🔄 Процесс разработки

### Git Flow

Мы используем упрощенный Git Flow:

- `main` - основная стабильная ветка
- `feature/*` - новые функции
- `fix/*` - исправления багов
- `docs/*` - изменения в документации
- `refactor/*` - рефакторинг кода
- `test/*` - добавление тестов

### Commit Messages

Мы следуем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы:**
- `feat` - новая функция
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor` - рефакторинг кода
- `test` - добавление или изменение тестов
- `chore` - изменения в сборке, инструментах и т.д.

**Примеры:**

```
feat(research): добавлена поддержка региональных исследований

Реализована возможность указания конкретного региона РФ
для более точного анализа рынка.

Closes #123
```

```
fix(api): исправлена ошибка валидации даты

Исправлена проблема с парсингом даты в формате ISO 8601.

Fixes #456
```

## 📝 Стандарты кодирования

### Python (Backend)

Мы следуем [PEP 8](https://www.python.org/dev/peps/pep-0008/) с некоторыми дополнениями:

```python
# Используйте type hints
def calculate_market_size(region: str, industry: str) -> float:
    """
    Рассчитывает размер рынка.

    Args:
        region: Код региона РФ
        industry: Название отрасли

    Returns:
        Размер рынка в рублях

    Raises:
        ValueError: Если регион или отрасль не найдены
    """
    pass

# Используйте f-strings
message = f"Анализ рынка {industry} в регионе {region}"

# Константы в UPPER_CASE
MAX_RETRIES = 3
API_TIMEOUT = 30

# Классы в CamelCase
class MarketAnalyzer:
    pass

# Функции и переменные в snake_case
def get_market_data():
    market_data = {}
    return market_data
```

**Инструменты для проверки:**

```bash
# Форматирование кода
black backend/

# Сортировка импортов
isort backend/

# Проверка типов
mypy backend/

# Линтинг
ruff check backend/

# Всё вместе
black backend/ && isort backend/ && mypy backend/ && ruff check backend/
```

### TypeScript (Frontend)

Мы следуем [Vue 3 Style Guide](https://vuejs.org/style-guide/):

```typescript
// Используйте Composition API
import { ref, computed, onMounted } from 'vue'

// Используйте типы
interface Research {
  id: string
  title: string
  status: 'pending' | 'processing' | 'completed'
}

// Компоненты в PascalCase
export default defineComponent({
  name: 'ResearchList',
  // ...
})

// Composables с префиксом use
export function useResearch() {
  const researches = ref<Research[]>([])

  const completedResearches = computed(() =>
    researches.value.filter(r => r.status === 'completed')
  )

  return {
    researches,
    completedResearches
  }
}
```

**Инструменты для проверки:**

```bash
# Линтинг
npm run lint

# Проверка типов
npm run type-check

# Форматирование
npm run format
```

## 🧪 Тестирование

### Backend тесты

```bash
cd backend

# Запуск всех тестов
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Конкретный тест
pytest tests/test_research.py

# С выводом print
pytest -s

# Параллельно
pytest -n auto
```

**Структура тестов:**

```python
import pytest
from fastapi.testclient import TestClient

def test_create_research(client: TestClient, db_session):
    """Тест создания исследования."""
    # Arrange
    payload = {
        "title": "Тест",
        "product_description": "Описание"
    }

    # Act
    response = client.post("/api/v1/researches", json=payload)

    # Assert
    assert response.status_code == 201
    data = response.json()
    assert data["title"] == "Тест"
```

### Frontend тесты

```bash
cd frontend

# Unit тесты (Vitest)
npm run test

# С покрытием
npm run test:coverage

# E2E тесты (Playwright)
npm run test:e2e

# E2E с UI
npm run test:e2e:ui

# Watch mode
npm run test -- --watch
```

**Структура тестов:**

```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import ResearchList from './ResearchList.vue'

describe('ResearchList', () => {
  it('отображает список исследований', () => {
    const wrapper = mount(ResearchList, {
      props: {
        researches: [
          { id: '1', title: 'Тест 1' },
          { id: '2', title: 'Тест 2' }
        ]
      }
    })

    expect(wrapper.findAll('.research-item')).toHaveLength(2)
  })
})
```

### Минимальные требования для PR

- ✅ Все существующие тесты проходят
- ✅ Новый код покрыт тестами (минимум 80%)
- ✅ Линтеры не выдают ошибок
- ✅ Проверка типов проходит успешно

## 📚 Документация

### Когда обновлять документацию

- При добавлении новой функции
- При изменении API
- При изменении процесса установки/развертывания
- При исправлении существующей документации

### Стиль документации

- Используйте Markdown
- Добавляйте примеры кода
- Используйте скриншоты где необходимо
- Пишите понятно и кратко

## 🔍 Процесс ревью кода

### Что проверяется

1. **Функциональность** - работает ли код как ожидается
2. **Тесты** - есть ли тесты и проходят ли они
3. **Стиль кода** - соответствует ли стандартам
4. **Документация** - обновлена ли документация
5. **Безопасность** - нет ли уязвимостей
6. **Производительность** - нет ли проблем с производительностью

### Как ускорить ревью

- Делайте небольшие PR (до 400 строк)
- Пишите понятные commit messages
- Добавляйте описание в PR
- Отвечайте на комментарии быстро
- Запускайте тесты и линтеры локально

### Пример хорошего PR

```markdown
## Описание
Добавлена функция экспорта отчета в Excel формат.

## Мотивация
Пользователи просили возможность экспортировать отчеты в Excel
для дальнейшего анализа (#789).

## Изменения
- Добавлен новый эндпоинт `/api/v1/reports/{id}/export/excel`
- Реализован генератор Excel файлов с openpyxl
- Добавлены тесты для экспорта
- Обновлена документация API

## Тестирование
- [ ] Юнит тесты прошли
- [ ] Интеграционные тесты прошли
- [ ] Протестировано вручную
- [ ] Линтеры не выдают ошибок

## Скриншоты
[Прикрепите скриншоты если изменяли UI]

## Связанные issues
Closes #789
```

## 🐛 Отчеты о багах

При создании issue о баге, пожалуйста, включите:

1. **Описание** - что пошло не так
2. **Шаги воспроизведения** - как воспроизвести баг
3. **Ожидаемое поведение** - что должно было произойти
4. **Фактическое поведение** - что произошло
5. **Окружение** - ОС, версия Python/Node, браузер
6. **Скриншоты/логи** - если применимо

## 💡 Предложения новых функций

При предложении новой функции:

1. Создайте Discussion (не Issue)
2. Опишите проблему, которую решает функция
3. Предложите возможные решения
4. Опишите альтернативы
5. Дождитесь обратной связи перед началом реализации

## 📞 Связь

- **GitHub Issues** - для багов и задач
- **GitHub Discussions** - для обсуждений и вопросов
- **Pull Requests** - для предложения изменений

## 🙏 Благодарности

Спасибо всем контрибьюторам за вклад в проект!

---

Если у вас есть вопросы, не стесняйтесь спрашивать в Discussions.

**Счастливого кодирования! 🚀**
